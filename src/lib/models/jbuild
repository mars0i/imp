(jbuild_version 1)

(library 
 ((name        models)
  (public_name models)
  (modules (wrightfisher setchains tranmats credalsetPDF))
  (libraries (batteries core owl parmap utils))
  ;(ocamlopt_flags "-O3")
  ))
