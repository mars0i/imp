(jbuild_version 1)

(library 
 ((name        utils)
  (public_name utils)
  (modules (prob genl))
  (libraries (batteries core owl))
  ;(ocamlopt_flags (:standard)) -dcmm -O3 ; -dcmm dumps intermediate rep
  ))
